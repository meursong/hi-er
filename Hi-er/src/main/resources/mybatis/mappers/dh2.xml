<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mulcam.hier.dao.ReviewDAO">

	<select id="selectReview" resultType="Review" parameterType="int">
		<![CDATA[
			select r.review_wid, r.product_id, r.seller_id, r.content, r.star_point, r.createdAt, u.nickname from review r left join user u on u.user_id=r.review_wid order by review_id desc;
		]]>
	</select>
	
</mapper>